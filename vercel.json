{
  "installCommand": "pnpm install",
  "buildCommand": "cd client && pnpm exec vite build --outDir ../dist/public",
  "outputDirectory": "dist/public",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
